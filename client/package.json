{
  "name": "macbooklister",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://kullaniciadi.github.io/macbookprices",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && gh-pages -d out -t true"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.3",
    "next": "10.0.9",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-table": "^7.6.3"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
